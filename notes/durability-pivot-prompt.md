# Durability pivot prompt

Preserved verbatim from the request that replaced the original feature-tour
sequence:

> okay good progress. but now i think we should change the demo script completely to make in focused on durability instead of showing a bag of features. we're going to first show an agent that submits PRs from a slack message, to set the stage - eg how you'd build such an agent? there's nothing we need to build for this yet (ok another session is fixing working repos but that's separate). the demo will be similarly structured to our current code example slides, just different content. first we'll show the naive way - here's how you might build it: put claude code in the sandbox, check out the repo there, give it some instructions, start it. so that will be the first code example (needs to be rather brief to stay legible but we want it actually working). run the code, it works. show the sandbox running in another tab. there will be more slides after (for problems with this approach, reliability, security, etc) but for now lets just build the first one against real OC APIs and get smth working end to end (no mocks)
