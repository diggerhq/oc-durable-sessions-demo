---- NEW TASK AGAIN ---- i now want to create a demo app for a very particular demo flow; we'll need to make a new repo for that. the demo app i will be running locally, mostly frontend. it's going to show some bits of code front and center, with appropriate outputs and work live against OC public APIs. as we build it we might want to change / improve some of the apis (most likely session-api, its about durable agent sessions) and if smth isn't (yet) available we should make some mocks first and then document the missing bits to impl later. first i want to arrive at a coherent demo sequence and record a video of it to share with the team for feedback, then we build the missing parts of the underlying infra. im thinking the following sequence roughly. first show the most minimal code example to start an agent session via SDK; click run and it'd show some response data. then i'll switch tabs to show new session in OpenComputer how it produces outputs etc. then I say - here its very much like Claude Managed Agents; but what if i want to use a different model? I can! go back to my demo app, press "codex" button and it updates example code with codex runtime and i do the same and show output again. then I say - ok that's nice for a small action but what if i'm building an agent that is a little less trivial? do I pass the prompts and skills for every session? (show example updated with some prompts, skills etc - big chunk of code now) - then say, no, i can actually define my agent once, and every session inherits the system prompt and skills (click agent button, example is small again with agent id). then show OC dashboard with agent definition. say, ok, here's my system prompt, and here I can upload zips. but how do I keep track of changes to my prompt and skills? (edit the prompt, save) - if I edit my prompt or upload new skills, I get a new revision; but doing it in the dasbhoard can get messy very quickly, you cannot see who did what. so I can just connect a repo to take my agent's system prompt and skills from (make new agent from a repo with just skills). back to the demo app, click button smth like "agent from repo" - code example changes a bit (different agent id), example still very small; click run, it shows some output (session created, maybe with a link to session - actually good for all previous and future examples to to include link to the session). then I say: that's why an agent is a repo; now every time i push a commit (actually push a commit in github tab), there's a new revision of my agent. neat. then say : BUT - this is still just prompt and skills on top of claude code or codex as-is. what if I want some custom behaviours from my agent? what if i have a custom harness, or using an agent framework? i can do that too! here I have an agent made with Flue framework; it uses Pi harness under the hood. i can deploy it from a repo (show repo) or from a CLI (sho oc agent deploy). and now that it's deployed - i can call it just like a simple prompt agent (click the final example in the demo app, show output, show new session with responses). so - now I want to build this demo app to record an aspirational demo video draft first, and document the missing session-api bits if any. lets start by making the app repo right away with some sort of a draft and keep notes in markdown files in the same repo. also put this prompt as-is into the new repo.

